services:
  server-app:
    build: 
      context: ./Server
      dockerfile: Dockerfile
    container_name: server_node
    ports:
      - "8080:8080" 

  client-app:
    build:
      context: ./Client
      dockerfile: Dockerfile
    container_name: client_node
    depends_on:
      - server-app