<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Стартовая активность должна быть exported="true" -->
        <activity android:name=".ui.liners.LinersListActivity" android:exported="false">

        </activity>
        <activity android:name=".ui.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.linersRoutes.LinersRoutesActivity" android:exported="false"/>

        <!-- Остальные активности можно оставить закрытыми -->
        <activity android:name=".ui.routes.RoutesListActivity" android:exported="false"/>
        <activity android:name=".ui.routes.RouteDetailActivity" android:exported="false"/>
        <activity android:name=".ui.routes.RouteEditActivity" android:exported="false"/>
        <activity android:name=".ui.liners.LinerDetailActivity" android:exported="false"/>
        <activity android:name=".ui.liners.LinerEditActivity" android:exported="false"/>

    </application>
</manifest>
